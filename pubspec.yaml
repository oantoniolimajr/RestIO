name: restio
description: A Rest client for Desktop.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.5.0
  flutter_bloc: ^8.1.6
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  pretty_json_view: ^0.0.6
  google_fonts: ^8.1.0
  flutter_json_view: ^1.1.5
  uuid: ^4.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
